import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import type{FiscalSyncRun}from"../api";
export function FiscalSyncRunsTable({runs,isLoading,canReprocess,reprocessing,onReprocess}:{runs:FiscalSyncRun[];isLoading:boolean;canReprocess:boolean;reprocessing:boolean;onReprocess:(id:string)=>void}){return <Card><CardHeader><CardTitle>Execuções fiscais</CardTitle></CardHeader><CardContent>{isLoading?<p role="status">Carregando execuções…</p>:<div className="grid gap-3">{runs.map(run=><article key={run.id} className="grid gap-2 rounded-lg border p-3 md:grid-cols-[1fr_auto_auto]"><div><strong>{run.client_name||"Cliente removido"}</strong><p className="text-sm text-muted-foreground">{run.capability_key} · {new Date(run.created_at).toLocaleString("pt-BR")}</p>{run.error_summary?<p role="alert" className="text-sm">{run.error_summary}</p>:null}</div><span role="status">{run.status}</span>{canReprocess&&run.eligible_reprocess?<Button size="sm" variant="outline" disabled={reprocessing} onClick={()=>onReprocess(run.id)}>Reprocessar</Button>:null}</article>)}</div>}</CardContent></Card>}
