import type {FiscalAuthorizationContext,TaxIdentifier} from "./provider.ts";
export type AuthorizationInput={organizationId:string;clientId?:string;connectionId:string;capabilityKey:string;contractor:TaxIdentifier;author:TaxIdentifier;taxpayer:TaxIdentifier};
export async function resolveAuthorization(input:AuthorizationInput,checks:{organization:(id:string)=>Promise<boolean>;client:(org:string,id:string)=>Promise<boolean>;capability:(org:string,key:string)=>Promise<boolean>}):Promise<FiscalAuthorizationContext>{if(!await checks.organization(input.organizationId))throw new Error("ORGANIZATION_NOT_AVAILABLE");if(input.clientId&&!await checks.client(input.organizationId,input.clientId))throw new Error("CLIENT_NOT_AVAILABLE");if(!await checks.capability(input.organizationId,input.capabilityKey))throw new Error("FEATURE_DISABLED");return {connectionId:input.connectionId,organizationId:input.organizationId,clientId:input.clientId,contractor:input.contractor,requestAuthor:input.author,taxpayer:input.taxpayer};}
