export type FiscalErrorCode="SERPRO_AUTHENTICATION"|"SERPRO_AUTHORIZATION"|"SERPRO_VALIDATION"|"SERPRO_RATE_LIMIT"|"SERPRO_TEMPORARY"|"SERPRO_BUSINESS"|"SERPRO_TIMEOUT"|"SERPRO_UNAVAILABLE"|"PROCURATION_REQUIRED"|"CERTIFICATE_INVALID"|"EXTERNAL_CONTRACT_UNVERIFIED";
export class FiscalIntegrationError extends Error { constructor(public code:FiscalErrorCode, public retryable:boolean, public requiresAction:boolean, public correlationId:string, public requestId:string){ super(code); this.name="FiscalIntegrationError"; } }
export function classifyHttpStatus(status:number,correlationId:string,requestId:string){ if(status===401)return new FiscalIntegrationError("SERPRO_AUTHENTICATION",true,false,correlationId,requestId); if(status===403)return new FiscalIntegrationError("SERPRO_AUTHORIZATION",false,true,correlationId,requestId); if(status===429)return new FiscalIntegrationError("SERPRO_RATE_LIMIT",true,false,correlationId,requestId); if(status===400||status===404)return new FiscalIntegrationError("SERPRO_VALIDATION",false,false,correlationId,requestId); return new FiscalIntegrationError("SERPRO_TEMPORARY",status>=500,false,correlationId,requestId); }
