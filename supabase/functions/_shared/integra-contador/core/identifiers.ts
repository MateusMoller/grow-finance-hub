import type { TaxIdentifier } from "./provider.ts";
function digits(value:string){ return value.replace(/\D/g,""); }
function validCheck(value:string,size:number){ if(value.length!==size||/^(\d)\1+$/.test(value)) return false; const calc=(base:string,weights:number[])=>{const sum=[...base].reduce((n,c,i)=>n+Number(c)*weights[i],0); const r=sum%11; return r<2?0:11-r}; if(size===11){const d1=calc(value.slice(0,9),[10,9,8,7,6,5,4,3,2]); const d2=calc(value.slice(0,9)+d1,[11,10,9,8,7,6,5,4,3,2]); return value.endsWith(`${d1}${d2}`)} const weight=(length:number)=>Array.from({length},(_,i)=>((length-i-1)%8)+2); const d1=calc(value.slice(0,12),weight(12)); const d2=calc(value.slice(0,12)+d1,weight(13)); return value.endsWith(`${d1}${d2}`)}
export function normalizeTaxIdentifier(value:string):TaxIdentifier { const normalized=digits(value); const type=normalized.length===11?"CPF":normalized.length===14?"CNPJ":null; if(!type||!validCheck(normalized,normalized.length)) throw new Error("INVALID_TAX_IDENTIFIER"); return {type,value:normalized}; }
