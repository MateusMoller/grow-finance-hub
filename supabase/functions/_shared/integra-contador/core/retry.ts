export type RetryDecision={retry:boolean;delayMs:number;waitingExternal:boolean};
export function retryDecision(status:number,attempt:number,providerWaitMs?:number):RetryDecision { if(status===202||status===204)return {retry:true,delayMs:Math.max(3000,providerWaitMs??3000),waitingExternal:true}; if(status===401)return {retry:attempt===0,delayMs:0,waitingExternal:false}; if(status===429||status>=500){const base=Math.min(300000,1000*2**attempt);return {retry:attempt<5,delayMs:base+Math.floor(Math.random()*Math.max(1,base/4)),waitingExternal:false}} return {retry:false,delayMs:0,waitingExternal:false}; }
