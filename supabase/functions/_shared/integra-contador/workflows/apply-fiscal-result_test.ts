import{assertEquals}from"https://deno.land/std@0.224.0/assert/mod.ts";import{classifyFiscalResult}from"./apply-fiscal-result.ts";
Deno.test("routes no-action, task, conclusive and ambiguous results",()=>{assertEquals(classifyFiscalResult({issueFingerprint:"1"}).kind,"no_action");assertEquals(classifyFiscalResult({issueFingerprint:"2",actionRequired:true,humanAction:"Regularizar procuração"}).kind,"task");assertEquals(classifyFiscalResult({issueFingerprint:"3",obligationMatch:{instanceId:"i",confidence:.98}}).kind,"obligation");assertEquals(classifyFiscalResult({issueFingerprint:"4",obligationMatch:{instanceId:"i",confidence:.7}}).kind,"review")});
Deno.test("stable keys deduplicate and inactive clients create no work",()=>{const a=classifyFiscalResult({issueFingerprint:"same",actionRequired:true,humanAction:"Agir"});const b=classifyFiscalResult({issueFingerprint:"same",actionRequired:true,humanAction:"Agir"});assertEquals(a.integrationKey,b.integrationKey);assertEquals(classifyFiscalResult({issueFingerprint:"x",clientStatus:"Inativo",actionRequired:true,humanAction:"Agir"}).kind,"no_action")});
